C51 COMPILER V9.60.0.0   MCU_COMMONMETHOD                                                  12/13/2020 19:26:55 PAGE 1   


C51 COMPILER V9.60.0.0, COMPILATION OF MODULE MCU_COMMONMETHOD
OBJECT MODULE PLACED IN .\Objects\MCU_CommonMethod.obj
COMPILER INVOKED BY: D:\Keil5\C51\BIN\C51.EXE ..\Example\MCU_CommonMethod.c LARGE OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTE
                    -XTEND PRINT(.\Listings\MCU_CommonMethod.lst) TABS(2) OBJECT(.\Objects\MCU_CommonMethod.obj)

line level    source

   1          #include "MCU_CommonMethod.h"
   2          
   3          //========================================================================
   4          // 函数: void Delay_ms(unsigned int time)
   5          // 描述: 软件延时函数
   6          // 参数: time:延时时间
   7          // 返回: 无
   8          // 说明：单片机频率不同，延时效果不同
   9          // 版本: V1.0, 2020.03.09
  10          //========================================================================
  11          void Delay_ms(unsigned int time)
  12          {
  13   1          unsigned int x, y;
  14   1          for(x = time; x > 0; x--)
  15   1              for(y = 110; y > 0; y--);
  16   1      }
  17          //========================================================================
  18          // 函数: void memcat(unsigned char *dest, const unsigned char *src,unsigned int index,len)
  19          // 描述: 字符数组拼接函数
  20          // 参数: dest目的字符数组,src源字符数组,index拼接位置,len拼接长度
  21          // 返回: 无
  22          // 说明：此函数主要初始化串口设置参数
  23          // 版本: V1.0, 2020.03.09
  24          //========================================================================
  25          void memcat(unsigned char *dest, const unsigned char *src,unsigned int index, len)
  26          {
  27   1          unsigned int i=0;
  28   1          if(index<0){ return;}
  29   1          for (i = index;  i< len+index; i++) {
  30   2              dest[i]=src[i-index];
  31   2          }
  32   1      }
  33          
  34          //========================================================================
  35          // 函数: void UART_BUF_Init()
  36          // 描述: 初始化串口接收相关参数或设置
  37          // 参数: 无
  38          // 返回: 无
  39          // 说明：此函数主要初始化串口设置参数
  40          // 版本: V1.0, 2020.03.09
  41          //========================================================================
  42          void UART_BUF_Init()
  43          {
  44   1          RX1_len = 0;
  45   1          memset(RX1_Buffer, 0, sizeof(RX1_Buffer));
  46   1      }
  47          //========================================================================
  48          // 函数: unsigned char Calculate_Checksum(unsigned char *Buf,unsigned int Buf_len)
  49          // 描述: 计算校验和
  50          // 参数: *Buf:字符数组,Buf_len:数组实际长度
  51          // 返回: 无
  52          // 说明：无
  53          // 版本: V1.0, 2020.03.09
  54          //========================================================================
C51 COMPILER V9.60.0.0   MCU_COMMONMETHOD                                                  12/13/2020 19:26:55 PAGE 2   

  55          unsigned char Calculate_Checksum(unsigned char *Buf, unsigned int Buf_len)
  56          {
  57   1          unsigned char checksum = 0x00;
  58   1          unsigned int i = 0, len = Buf_len;
  59   1          for(i = 2; i < (len - 1); i++) {
  60   2              checksum = checksum + Buf[i];
  61   2          }
  62   1          checksum = checksum % 256;
  63   1          return checksum;
  64   1      }
  65          
  66          //========================================================================
  67          // 函数: unsigned char Check_Protocol(unsigned char *Buf,unsigned int Buf_len)
  68          // 描述: 检验校验和是否一致
  69          // 参数: *Buf:字符数组,Buf_len:数组实际长度
  70          // 返回: 无
  71          // 说明：具体思想为：接收到的BUF，取校验和，计算新的校验和，对比是否一致，若不一致，则数据无效
  72          // 版本: V1.0, 2020.03.09
  73          //========================================================================
  74          unsigned char Check_Protocol(unsigned char *Buf, unsigned int Buf_len)
  75          {
  76   1          unsigned char Check_protocol_flag = 0;
  77   1          //将原BUF的校验和提取
  78   1          unsigned char old_checksum = 0xFF;
  79   1          unsigned char new_checksum = 0x00;
  80   1          if(Buf_len>0){old_checksum = Buf[Buf_len - 1];}
  81   1          //计算新的校验和并赋值给new_checksum
  82   1          new_checksum = Calculate_Checksum(Buf, Buf_len);
  83   1          //比较原校验和与新校验和是否一致
  84   1          if(new_checksum == old_checksum) {
  85   2              Check_protocol_flag = 1;
  86   2          }
  87   1          return Check_protocol_flag;
  88   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    365    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----      24
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
